# Thermograph
Thermodynamic application and UI layer built on top of `zgraph`.

`thermograph` acts as the bridge between human-readable thermodynamic concepts (Elements, Microstates, Enthalpies, Phases) and the strict, tensor-only requirements of the `zgraph` math engine.
